<!DOCTYPE html>
<html lang="es">

{{> headers }}

<body class="min-h-screen flex flex-col">

  {{> navbar }}

  <main class="flex-1 flex flex-col items-center px-4 py-10">
    <div class="w-full md:w-[80%] flex flex-wrap justify-center gap-6 mt-10">
      {{#each datos}}
      {{> card}}
      {{/each}}
    </div>
    <a href="/producto/nuevo"
      class="fixed bottom-8 right-8 w-16 h-16 bg-primary text-white rounded-full flex items-center justify-center text-4xl shadow-lg hover:bg-primary-focus transition">
      +
    </a>
  </main>

  {{> footer }}

</body>

</html>